O trabalho foi implementado na linguagem C padrão, no ambiente Linux.

Para compilar os arquivos de código fonte, as seguintes instruções devem ser
usadas dentro do diretório corrente, no terminal:

Soma máxima:

gcc -O2 somamax.c -o somamax

Quadrado mágico:

gcc -O2 qmagico.c -o qmagico